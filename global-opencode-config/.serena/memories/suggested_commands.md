Useful commands for this workspace:
- `ls`, `pwd`, `git status`, `git diff`, `git log`
- `grep`/`rg` or OpenCode search tools for locating instruction text
- `node`, `npm`, `bun` for local scripts/config validation
- `python3 ~/.config/opencode/scripts/...` or shell scripts in `scripts/` for GitHub/board sync flows
- `jq` or JSON-aware tooling for validating `opencode.json`
- OpenCode/Serena semantic tools for targeted file discovery and editing
Darwin environment notes: standard BSD userland applies; prefer absolute paths under `/Users/jeremy/.config/opencode` for config work.