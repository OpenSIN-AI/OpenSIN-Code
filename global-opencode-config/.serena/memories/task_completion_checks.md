For config/instruction changes in `~/.config/opencode`, finish by validating the changed JSON/Markdown surfaces directly. Typical checks: parse `opencode.json` as valid JSON, search for old instruction text to confirm removal, inspect neighboring instruction files for consistency, and run project-specific scripts only when they are directly relevant. Do not auto-commit unless the user explicitly asks.