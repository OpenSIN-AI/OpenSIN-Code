Conventions in `~/.config/opencode` are instruction- and config-heavy rather than application-code-heavy. Most edits should preserve existing JSON structure, Markdown hierarchy, and instruction tone unless the task explicitly asks for governance changes. Prefer targeted edits in `opencode.json`, `AGENTS.md`, skill docs, and scripts instead of broad rewrites. Keep config values valid JSON, keep Markdown readable with stable headings/lists, and preserve existing naming such as `AGENTS.md`, `SKILL.md`, and tool/skill identifiers.