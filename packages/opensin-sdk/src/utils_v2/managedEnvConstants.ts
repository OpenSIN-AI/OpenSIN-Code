/**
 * Environment variables that control inference routing: which provider to use,
 * which endpoint to hit, and which model IDs to send.
 *
 * When OPENSIN_CODE_PROVIDER_MANAGED_BY_HOST is truthy in the spawn env, these
 * are stripped from settings-sourced env so the host's routing config isn't
 * overridden by a user's ~/.opensin/settings.json — e.g. a Bedrock setup for
 * terminal CLI that would break a host that only supports first-party auth.
 *
 * @[MODEL LAUNCH]: New models usually don't need changes here —
 * VERTEX_REGION_OPENSIN_* is prefix-matched. New providers or new routing
 * config vars (endpoint, project, region, auth) do.
 */
const PROVIDER_MANAGED_ENV_VARS = new Set([
  // The flag itself — settings can't unset it once the host set it
  'OPENSIN_CODE_PROVIDER_MANAGED_BY_HOST',
  // Provider selection
  'OPENSIN_CODE_USE_BEDROCK',
  'OPENSIN_CODE_USE_VERTEX',
  'OPENSIN_CODE_USE_FOUNDRY',
  // Endpoint config (base URLs, project/resource identifiers)
  'OPENSIN_BASE_URL',
  'OPENSIN_BEDROCK_BASE_URL',
  'OPENSIN_VERTEX_BASE_URL',
  'OPENSIN_FOUNDRY_BASE_URL',
  'OPENSIN_FOUNDRY_RESOURCE',
  'OPENSIN_VERTEX_PROJECT_ID',
  // Region routing (per-model VERTEX_REGION_OPENSIN_* handled by prefix below)
  'CLOUD_ML_REGION',
  // Auth
  'OPENSIN_API_KEY',
  'OPENSIN_AUTH_TOKEN',
  'OPENSIN_CODE_OAUTH_TOKEN',
  'AWS_BEARER_TOKEN_BEDROCK',
  'OPENSIN_FOUNDRY_API_KEY',
  'OPENSIN_CODE_SKIP_BEDROCK_AUTH',
  'OPENSIN_CODE_SKIP_VERTEX_AUTH',
  'OPENSIN_CODE_SKIP_FOUNDRY_AUTH',
  // Model defaults — often set to provider-specific ID formats
  'OPENSIN_MODEL',
  'OPENSIN_DEFAULT_HAIKU_MODEL',
  'OPENSIN_DEFAULT_HAIKU_MODEL_DESCRIPTION',
  'OPENSIN_DEFAULT_HAIKU_MODEL_NAME',
  'OPENSIN_DEFAULT_HAIKU_MODEL_SUPPORTED_CAPABILITIES',
  'OPENSIN_DEFAULT_OPUS_MODEL',
  'OPENSIN_DEFAULT_OPUS_MODEL_DESCRIPTION',
  'OPENSIN_DEFAULT_OPUS_MODEL_NAME',
  'OPENSIN_DEFAULT_OPUS_MODEL_SUPPORTED_CAPABILITIES',
  'OPENSIN_DEFAULT_SONNET_MODEL',
  'OPENSIN_DEFAULT_SONNET_MODEL_DESCRIPTION',
  'OPENSIN_DEFAULT_SONNET_MODEL_NAME',
  'OPENSIN_DEFAULT_SONNET_MODEL_SUPPORTED_CAPABILITIES',
  'OPENSIN_SMALL_FAST_MODEL',
  'OPENSIN_SMALL_FAST_MODEL_AWS_REGION',
  'OPENSIN_CODE_SUBAGENT_MODEL',
])

const PROVIDER_MANAGED_ENV_PREFIXES = [
  // Per-model Vertex region overrides — scales with model releases, so
  // prefix-matched to avoid drift on each launch.
  'VERTEX_REGION_OPENSIN_',
]

export function isProviderManagedEnvVar(key: string): boolean {
  const upper = key.toUpperCase()
  return (
    PROVIDER_MANAGED_ENV_VARS.has(upper) ||
    PROVIDER_MANAGED_ENV_PREFIXES.some(p => upper.startsWith(p))
  )
}

/**
 * Dangerous shell settings that can execute arbitrary shell code
 */
export const DANGEROUS_SHELL_SETTINGS = [
  'apiKeyHelper',
  'awsAuthRefresh',
  'awsCredentialExport',
  'gcpAuthRefresh',
  'otelHeadersHelper',
  'statusLine',
] as const

/**
 * Safe environment variables that can be applied before trust dialog.
 * These are OpenSIN Code specific settings that don't pose security risks.
 *
 * IMPORTANT: This is the source of truth for which env vars are safe.
 * Any env var NOT in this list is considered dangerous and will trigger
 * a security dialog when set via remote managed settings.
 *
 * Dangerous env vars (NOT in this list):
 *
 * === REDIRECT TO ATTACKER-CONTROLLED SERVER ===
 * - OPENSIN_BASE_URL, OPENSIN_BEDROCK_BASE_URL, OPENSIN_FOUNDRY_BASE_URL, OPENSIN_VERTEX_BASE_URL
 * - HTTP_PROXY, HTTPS_PROXY, NO_PROXY, http_proxy, https_proxy, no_proxy
 * - OTEL_EXPORTER_OTLP_ENDPOINT, OTEL_EXPORTER_OTLP_LOGS_ENDPOINT, OTEL_EXPORTER_OTLP_METRICS_ENDPOINT
 *
 * === TRUST ATTACKER-CONTROLLED SERVER ===
 * - NODE_TLS_REJECT_UNAUTHORIZED
 * - NODE_EXTRA_CA_CERTS
 *
 * === SWITCH TO ATTACKER-CONTROLLED PROJECT ===
 * - OPENSIN_FOUNDRY_RESOURCE
 * - OPENSIN_API_KEY, OPENSIN_AUTH_TOKEN
 * - AWS_BEARER_TOKEN_BEDROCK
 */
export const SAFE_ENV_VARS = new Set([
  'OPENSIN_CUSTOM_HEADERS',
  'OPENSIN_CUSTOM_MODEL_OPTION',
  'OPENSIN_CUSTOM_MODEL_OPTION_DESCRIPTION',
  'OPENSIN_CUSTOM_MODEL_OPTION_NAME',
  'OPENSIN_DEFAULT_HAIKU_MODEL',
  'OPENSIN_DEFAULT_HAIKU_MODEL_DESCRIPTION',
  'OPENSIN_DEFAULT_HAIKU_MODEL_NAME',
  'OPENSIN_DEFAULT_HAIKU_MODEL_SUPPORTED_CAPABILITIES',
  'OPENSIN_DEFAULT_OPUS_MODEL',
  'OPENSIN_DEFAULT_OPUS_MODEL_DESCRIPTION',
  'OPENSIN_DEFAULT_OPUS_MODEL_NAME',
  'OPENSIN_DEFAULT_OPUS_MODEL_SUPPORTED_CAPABILITIES',
  'OPENSIN_DEFAULT_SONNET_MODEL',
  'OPENSIN_DEFAULT_SONNET_MODEL_DESCRIPTION',
  'OPENSIN_DEFAULT_SONNET_MODEL_NAME',
  'OPENSIN_DEFAULT_SONNET_MODEL_SUPPORTED_CAPABILITIES',
  'OPENSIN_FOUNDRY_API_KEY',
  'OPENSIN_MODEL',
  'OPENSIN_SMALL_FAST_MODEL_AWS_REGION',
  'OPENSIN_SMALL_FAST_MODEL',
  'AWS_DEFAULT_REGION',
  'AWS_PROFILE',
  'AWS_REGION',
  'BASH_DEFAULT_TIMEOUT_MS',
  'BASH_MAX_OUTPUT_LENGTH',
  'BASH_MAX_TIMEOUT_MS',
  'OPENSIN_BASH_MAINTAIN_PROJECT_WORKING_DIR',
  'OPENSIN_CODE_API_KEY_HELPER_TTL_MS',
  'OPENSIN_CODE_DISABLE_EXPERIMENTAL_BETAS',
  'OPENSIN_CODE_DISABLE_NONESSENTIAL_TRAFFIC',
  'OPENSIN_CODE_DISABLE_TERMINAL_TITLE',
  'OPENSIN_CODE_ENABLE_TELEMETRY',
  'OPENSIN_CODE_EXPERIMENTAL_AGENT_TEAMS',
  'OPENSIN_CODE_IDE_SKIP_AUTO_INSTALL',
  'OPENSIN_CODE_MAX_OUTPUT_TOKENS',
  'OPENSIN_CODE_SKIP_BEDROCK_AUTH',
  'OPENSIN_CODE_SKIP_FOUNDRY_AUTH',
  'OPENSIN_CODE_SKIP_VERTEX_AUTH',
  'OPENSIN_CODE_SUBAGENT_MODEL',
  'OPENSIN_CODE_USE_BEDROCK',
  'OPENSIN_CODE_USE_FOUNDRY',
  'OPENSIN_CODE_USE_VERTEX',
  'DISABLE_AUTOUPDATER',
  'DISABLE_BUG_COMMAND',
  'DISABLE_COST_WARNINGS',
  'DISABLE_ERROR_REPORTING',
  'DISABLE_FEEDBACK_COMMAND',
  'DISABLE_TELEMETRY',
  'ENABLE_TOOL_SEARCH',
  'MAX_MCP_OUTPUT_TOKENS',
  'MAX_THINKING_TOKENS',
  'MCP_TIMEOUT',
  'MCP_TOOL_TIMEOUT',
  'OTEL_EXPORTER_OTLP_HEADERS',
  'OTEL_EXPORTER_OTLP_LOGS_HEADERS',
  'OTEL_EXPORTER_OTLP_LOGS_PROTOCOL',
  'OTEL_EXPORTER_OTLP_METRICS_CLIENT_CERTIFICATE',
  'OTEL_EXPORTER_OTLP_METRICS_CLIENT_KEY',
  'OTEL_EXPORTER_OTLP_METRICS_HEADERS',
  'OTEL_EXPORTER_OTLP_METRICS_PROTOCOL',
  'OTEL_EXPORTER_OTLP_PROTOCOL',
  'OTEL_EXPORTER_OTLP_TRACES_HEADERS',
  'OTEL_LOG_TOOL_DETAILS',
  'OTEL_LOG_USER_PROMPTS',
  'OTEL_LOGS_EXPORT_INTERVAL',
  'OTEL_LOGS_EXPORTER',
  'OTEL_METRIC_EXPORT_INTERVAL',
  'OTEL_METRICS_EXPORTER',
  'OTEL_METRICS_INCLUDE_ACCOUNT_UUID',
  'OTEL_METRICS_INCLUDE_SESSION_ID',
  'OTEL_METRICS_INCLUDE_VERSION',
  'OTEL_RESOURCE_ATTRIBUTES',
  'USE_BUILTIN_RIPGREP',
  'VERTEX_REGION_OPENSIN_3_5_HAIKU',
  'VERTEX_REGION_OPENSIN_3_5_SONNET',
  'VERTEX_REGION_OPENSIN_3_7_SONNET',
  'VERTEX_REGION_OPENSIN_4_0_OPUS',
  'VERTEX_REGION_OPENSIN_4_0_SONNET',
  'VERTEX_REGION_OPENSIN_4_1_OPUS',
  'VERTEX_REGION_OPENSIN_4_5_SONNET',
  'VERTEX_REGION_OPENSIN_4_6_SONNET',
  'VERTEX_REGION_OPENSIN_HAIKU_4_5',
])
